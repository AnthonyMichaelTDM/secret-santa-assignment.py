reads `names.txt` and randomly assigns each person in that file to another person in that file

# instructions
- create a file, `names.txt` and populate it with names such that each name is on it's own line
- run `python3 main.py`
